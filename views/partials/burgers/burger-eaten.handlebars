<li class="list-group-item d-flex justify-content-between align-items-center text-secondary">
    <span class="badge badge-light badge-pill mr-2" data-devoured="{{devoured}}" data-id="{{id}}">{{id}}</span>
    {{burger_name}}
      <span class="ml-2"><button type="button" class="btn btn-dark mb-2 devour-btn" data-devoured="{{devoured}}" data-id="{{id}}">ADD</button></span>
</li>